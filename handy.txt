+ - - - - - - - - - - - - - - - - - +
For all the little commands I forget
+ - - - - - - - - - - - - - - - - - +

To run a particular tag
 
	cucumber features/test_script.feature -t @LOL

To run a particular line of a script (especially with feature outline tables)

	cucumber features/test_script.feature:42

To run a script with my current CUCUMBER setup and a particular browser

 cucumber features/core_functionality.feature BROWSER=firefox

To specify in one of my definition files which browser

	if page.driver.browser.browser == :chrome
	  page.should have_no_selector(:xpath, '//*[@id="mediaContainer"]/div[7]')
	end